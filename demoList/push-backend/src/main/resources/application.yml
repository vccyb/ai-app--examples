server:
  port: 8080

spring:
  application:
    name: push-system

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/push_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: chd19970227

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

# 推送平台配置
push:
  platforms:
    w3-todo:
      enabled: true
      api-url: http://localhost:8081/mock/w3-todo
    welink-app:
      enabled: true
      api-url: http://localhost:8081/mock/welink-app
